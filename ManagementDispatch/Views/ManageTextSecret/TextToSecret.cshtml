@model IEnumerable<ManagementDispatch.Models.CongVanDen>

<p style="font-size:20px">Số công văn đến: @Model.Count()</p>
<hr />
<hr />
<div class="row">
    <div class="col-lg-12">
        <div class="panel panel-default">

            <div class="panel-body">
                <table width="100%" class="table table-striped table-bordered table-hover" id="dataTables-example1">
                    <thead>
                        <tr>
                            <th>STT</th>
                            <th>Ngày nhận</th>
                            <th>Nơi gửi</th>
                            <th>Số CV</th>
                            <th>Loại CV</th>
                            <th>Ngày gửi</th>
                            <th>Nội dung</th>
                            <th>Người nhận</th>
                            <th>Phòng ban</th>
                            <th>File</th>
                            <th>Ảnh scan</th>
                            <th>Chức năng</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var item in Model)
                        {
                        <tr>
                            <td>@item.STT</td>
                            <td>@item.NgayNhan.ToString(string.Format("dd/MM/yyyy"))</td>
                            <td>@item.DonVi.TenDonVi</td>
                            <td>@item.IDCongVanDen</td>
                            <td>@item.LoaiCongVan.TenLoaiCongVan</td>
                            <td>@item.NgayGui.ToString(string.Format("dd/MM/yyyy"))</td>
                            <td>@item.NoiDung</td>
                            <td>@item.TenNguoiNhan</td>
                            <td>@item.PhongBan.TenPhongBan</td>
                            <td>@Html.ActionLink("Download", "Download", new { fileName = item.File })</td>
                            <td style="width:60px"> <img src="@Url.Content("~/"+item.AnhScan)" width="60px" height="50px"></td>
                            <td>
                                @Html.ActionLink("Sửa", "EditTextToSecret", new { id = item.IDCongVanDen })
                            </td>
                        </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
        <!-- /.panel -->
    </div>
    <!-- /.col-lg-12 -->
</div>
<script>
    $(document).ready(function () {
        $('#dataTables-example1').DataTable({
            responsive: true
        });
    });
</script>

