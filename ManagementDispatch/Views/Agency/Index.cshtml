@model IEnumerable<ManagementDispatch.Models.DonVi>
@{
    ViewBag.Title = "Các đơn vị quản lý";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

@section jsFooter{
    <script src="~/TemplateAdmin/controller/agencyController.js"></script>
}

<p style="font-size:30px">Các đơn vị quản lý</p>
<hr />
<div class="row">
    <div class="col-lg-12">
        <div class="panel panel-default">

            <p>
                @Html.ActionLink("Thêm mới", "AddAgency", "Admin", htmlAttributes: new { @class = "btn btn-primary" })
            </p>
            <div class="panel-body">
                <table width="100%" class="table table-striped table-bordered table-hover" id="dataTables-example">
                    <thead>
                        <tr>
                            <th>Tên đơn vị</th>
                            <th>Thông tin</th>
                            <th>Trạng thái</th>
                            <th>Chức năng</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var item in Model)
                        {
                            <tr id="rpw_@item.IDDonVi">
                                <td class="center">@item.TenDonVi</td>
                                <td class="center">@item.ThongTin</td>
                                <td><a href="#" class="abc" data-id="@item.IDDonVi">@((bool)item.isDelete ? "Đang sử dụng" : "Đã khóa")</a></td>
                                <td class="center">
                                    @Html.ActionLink("Sửa", "EditAgency", new { id = item.IDDonVi })
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
        <!-- /.panel -->
    </div>
    <!-- /.col-lg-12 -->
</div>
<script>
    $(document).ready(function () {
        $('#dataTables-example').DataTable({
            responsive: true
        });
    });
</script>